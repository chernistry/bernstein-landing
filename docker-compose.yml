name: bernstein-landing

services:
  bernstein-landing:
    build: .
    container_name: bernstein-landing
    restart: unless-stopped
    env_file:
      # On the VPS at /srv/bernstein-landing/app/.env, set:
      #   RESEND_API_KEY_BERNSTEIN=re_...
      # Optional override:
      #   BERNSTEIN_AUDIENCE_ID=REDACTED-AUDIENCE-ID-DA86
      - .env
    networks:
      - deploy_homepage

networks:
  deploy_homepage:
    external: true
